import {
    makeStyles
} from '@material-ui/core/styles';

const useStyles = makeStyles((theme) => ({
    card: {
        height: '100%',
    },
    image: {
        height: '100%',
    },
}));

export default useStyles;